#pragma once

#include "DesignerTypes.h"

#include <vector>

/** The product toolbox is derived exclusively from CUI built-in metadata. */
namespace DesignerControlCatalog
{
	std::vector<DesignerControlDescriptor> BuiltInDescriptors();
}
